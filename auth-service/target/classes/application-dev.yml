spring:
  datasource:
    url: jdbc:h2:mem:authdb;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
    driverClassName: org.h2.Driver
    username: sa
    password: ""
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false

server:
  port: 8081
security:
  jwt:
    secret: dev-secret-change-this-now-very-long-please-32b-min-abcdefghijkl
    expirationSeconds: 3600

